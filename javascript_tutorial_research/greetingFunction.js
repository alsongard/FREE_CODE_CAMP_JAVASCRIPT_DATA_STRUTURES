/*
4:00 am - 11:59 am = morning
12:00 pm - 4:59 pm = afternoon
5:00 pm
*/
/*
function greeting(name)
{
	const date = new Date();
	const hours = date.getHours();
	console.log(`The time is ${hours} and the date is ${date}`);
	
	let timeofDay;
	if(hours >= 4 , hours <=11:59)
	{
		timeofDay = "morning";
	}
	else if (hours >= 13 or hours <= 4:59)
	{
		timeofDay = "afternnon";
	}
	else if (hours >= 5 | hours <= 7:59)
	{
		timeofDay = "evening";
	}
	else
	{
		timeofDay = "night";
	}
	console.log(`Good ${timeofDay}, ${name} `}
}

greeting("gArd");
